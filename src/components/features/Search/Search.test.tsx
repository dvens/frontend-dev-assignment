import { render, fireEvent, screen } from '@testing-library/react';

describe('<Search />', () => {
    test('It should show reset button when it has a value', () => {});

    test('It should reset input value when reset button is clicked', () => {});
});
